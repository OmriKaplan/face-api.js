version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - ./public/models:/usr/share/nginx/html/models
    environment:
      - NODE_ENV=production